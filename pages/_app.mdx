import '../style.css'
import { Public_Sans, Fira_Code } from 'next/font/google'

export const publicSans = Public_Sans({
  subsets: ['latin'],
  display: 'swap'
})

export const firaCode = Fira_Code({
  subsets: ['latin'],
  display: 'swap'
})

export default function MyApp({ Component, pageProps }) {
  return (
    <>
      <style jsx global>{`
        body {
          font-family:
            ${publicSans.style.fontFamily},
            system-ui,
            -apple-system,
            BlinkMacSystemFont,
            'Segoe UI',
            Roboto,
            Oxygen,
            Ubuntu,
            Cantarell,
            'Helvetica Neue',
            Arial,
            sans-serif;
        }
        code,
        kbd,
        pre,
        samp {
          font-family:
            ${firaCode.style.fontFamily},
            ui-monospace,
            'SF Mono',
            Menlo,
            Consolas,
            Liberation Mono,
            monospace;
        }
      `}</style>
      <Component {...pageProps} />
    </>
  )
}
