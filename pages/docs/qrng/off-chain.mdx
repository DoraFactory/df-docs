---
title: 'Off-chain Faucet · QRNG Faucet'
desc: 'Dora Factory offers a faucet of QRNG (quantum random number generation) for scientific research and product testing. You can request device-independent quantum random numbers for free.'
---

import { Callout, Tabs, Tab } from 'nextra/components'

# Off-chain Faucet

## API endpoints

{/* prettier-ignore */}
<Tabs items={['HTTPS', 'WebSockets']}>
  <Tab>
    ```text
    https://qrng.dorafactory.org/faucet-api
    ```
  </Tab>
  <Tab>
    ```text
    wss://qrng.dorafactory.org/faucet-ws
    ```
  </Tab>
</Tabs>

## API reference

### `/random/GetRandomWords`

Get random numbers.

```bash
curl --request POST \
  --url $QRNG_API/random/GetRandomWords \
  --header 'Content-Type: application/json' \
  --data '{ "number": 1 }'
```

### `/random/GetCount`

Get the quntity of available random numbers.

```bash
curl --request POST \
  --url $QRNG_API/api/random/GetCount \
  --header 'Content-Type: application/json' \
  --data '{ }'
```
